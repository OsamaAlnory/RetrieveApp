<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RetrieveApp.Design.CardUser" HorizontalOptions="FillAndExpand"
             VerticalOptions="CenterAndExpand">
    <Button Style="{StaticResource Btn}" BackgroundColor="#51ff74"
            Text="Hämtat" FontFamily="{StaticResource MFont}"
            Clicked="ButtonRetrieved"/>
    <Button Style="{StaticResource Btn}" BackgroundColor="Crimson"
            Text="Ej hämtat" FontFamily="{StaticResource MFont}"
            Clicked="ButtonUnRetrieved"/>
</StackLayout>